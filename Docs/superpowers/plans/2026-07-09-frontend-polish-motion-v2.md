# Frontend Polish + Motion v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Make FaoTor mock-up feel alive, smooth, and Apple-like via motion polish without backend changes.

**Architecture:** Extend Motion v1 (`lib/motion.ts`, CSS tokens) with v2 variants and primitives; apply layer-by-layer from shell → dashboard → report → landing.

**Tech Stack:** Framer Motion, CSS motion tokens, existing Next.js 15 app.

---

See conversation plan for full phase breakdown (Phase 0–7).
